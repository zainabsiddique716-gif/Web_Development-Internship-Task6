{
  "name": "shipshape-focus-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A small, reviewable focus board for the combined branching and deployment exercise.",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "verify": "npm run lint && npm test && npm run build"
  }
}
